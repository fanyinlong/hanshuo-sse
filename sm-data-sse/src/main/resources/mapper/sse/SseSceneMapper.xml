<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shenmei.data.sse.mapper.SseSceneMapper">
    
    <resultMap type="SseScene" id="SseSceneResult">
        <result property="sceneId"    column="scene_id"    />
        <result property="sceneName"    column="scene_name"    />
        <result property="remark"    column="remark"    />
        <result property="modelId"    column="model_id"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectSseSceneVo">
        select scene_id, scene_name, remark, model_id, del_flag, create_by, create_time, update_by, update_time from sse_scene
    </sql>

    <select id="selectSseSceneList" parameterType="SseScene" resultMap="SseSceneResult">
        <include refid="selectSseSceneVo"/>
        <where>
            1=1
            <if test="sceneName != null  and sceneName != ''"> and scene_name like concat('%', #{sceneName}, '%')</if>
            <if test="modelId != null  and modelId != ''"> and model_id = #{modelId}</if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                AND create_time &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                AND create_time &lt;= #{params.endTime}
            </if>
        </where>
           AND del_flag = 0
    </select>
    
    <select id="selectSseSceneBySceneId" parameterType="Long" resultMap="SseSceneResult">
        <include refid="selectSseSceneVo"/>
        where scene_id = #{sceneId}
    </select>

    <insert id="insertSseScene" parameterType="SseScene" useGeneratedKeys="true" keyProperty="sceneId">
        insert into sse_scene
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="sceneName != null and sceneName != ''">scene_name,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="modelId != null and modelId != ''">model_id,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="sceneName != null and sceneName != ''">#{sceneName},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="modelId != null and modelId != ''">#{modelId},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateSseScene" parameterType="SseScene">
        update sse_scene
        <trim prefix="SET" suffixOverrides=",">
            <if test="sceneName != null and sceneName != ''">scene_name = #{sceneName},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            <if test="modelId != null and modelId != ''">model_id = #{modelId},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where scene_id = #{sceneId}
    </update>

    <delete id="deleteSseSceneBySceneId" parameterType="Long">
        delete from sse_scene where scene_id = #{sceneId}
    </delete>

    <delete id="deleteSseSceneBySceneIds" parameterType="String">
        update sse_scene set del_flag = 1  where scene_id in
        <foreach item="sceneId" collection="array" open="(" separator="," close=")">
            #{sceneId}
        </foreach>
    </delete>
</mapper>